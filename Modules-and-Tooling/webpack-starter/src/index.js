import message from './message';

console.log(message)

// we did then npm run buld in terminal,
// that way in console log it didnt show an error anymore, because
// it made main.js with bundled code